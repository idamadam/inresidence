<nav class="pagination mt2 dark-orange center" role="navigation">
    {{#if prev}}
        <a class="newer-posts orange" href="{{page_url prev}}">&larr; Newer Posts</a>
    {{/if}}
    <span class="page-number">Page {{page}} of {{pages}}</span>
    {{#if next}}
        <a class="older-posts orange" href="{{page_url next}}">Older Posts &rarr;</a>
    {{/if}}
</nav>